// Object destructuring example
const person = {
    name: 'Bruce Wayne',
    age: 36,
    location: 'Gotham City'
}

// Destructuring assignment - extracting properties from the object
const {name, age, location} = person;

// Logging the destructured variable
console.log(name) // Bruce Wayne